# Changelog

## 0.2.0

- 新增 `CursorFence-Portable.exe` 单文件便携版及 ZIP 发布包。
- 新增 `CursorFence-Installer.exe` 安装包：安装到当前用户后可从 Windows“已安装的应用”卸载和管理。
- 新增独立光标边界看门狗线程，在锁定期间以短间隔重新应用边界，减少高帧率游戏短暂释放 `ClipCursor` 时的越界窗口。
- 明确说明游戏可能覆盖系统光标边界，严格竞技游戏仍需自行评估兼容性。

## Unreleased

- 首个可分发版本：窗口/显示器边界锁定、可录制全局快捷键、通知区域常驻、单实例和当前用户开机启动。
- 稳定性修复：移除不安全的 ctypes 原生窗口过程热键回调，改用独立后台线程读取按键并通过队列交给 Tk；锁定键轮询间隔缩短到 8 ms，降低漏检概率。
- 修复 ScrollLock / CapsLock / NumLock 快捷键在后台按下后偶发无响应的问题。
- 修复“当前显示器”模式边界不稳定的问题：激活时捕获鼠标所在显示器，锁定期间保持该显示器范围。
- 修复运行中开启“键盘灯同步”后按键无效的问题：同步循环现在会随设置实时启动/停止，并持续校准锁定状态。
- 将依赖系统主题的复选框替换为统一的自绘滑动开关，避免出现黑色 `X`，并在禁用时提供清晰的视觉反馈。
- 状态切换通知默认开启，并新增开关；旧版本配置会自动按开启处理。
- 目录型打包进一步排除了用不到的 Pillow AVIF/WebP 插件，减少发布包体积和首次启动时的安全软件扫描负担。
- 显示器模式改用系统工作区边界，避开底部/侧边任务栏；状态通知会先覆盖旧提示并在约 2.6 秒后主动清理。
- 正式统一项目、源码、EXE、图标和配置名称为 CursorFence；发布包改为 `CursorFence-windows-x64.zip`。
- 补强多显示器坐标说明与回归测试：支持负坐标、不同分辨率、横竖屏和混合 DPI 排列；工作区边界不额外内缩，允许移动到工作区底边。
